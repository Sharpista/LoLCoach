# Release Readiness — LoLCoach MVP (Fase 8 / Hardening)

**Data da consolidação:** 2026-09-18T20:15Z (2026-09-18 22:15 CEST)
**SHA candidato:** `626081f3f9b6c81fe95576d9b8ad2f4aab50fd16`
**Base:** `origin/develop` @ `626081f` (`Merge pull request #21 from Sharpista/docs/009-hermes-skills`, 2026-09-18)
**Branch desta evidência:** `docs/release-readiness-626081f`
**Veredito consolidado da Fase 8:** APROVADO (QA) + APROVADO (code review) no mesmo candidato
**Ambiente de validação:** local — .NET 10.0.401, Node v24.21.0, npm 11.19.0, Linux 6.8.0-139-generic

---

## 1. Escopo e método

Esta evidência consolida a validação da Fase 8 (Release Readiness / Hardening do MVP),
executada em dois gates independentes sobre o **mesmo** SHA candidato:

| Gate | Card | Run | Perfil | Workspace | Branch |
|---|---|---|---|---|---|
| QA | `t_508033d0` | #85 | `qualidade` | `/home/alexandre/LoLSaas/.worktrees/t_508033d0` | `qa/release-readiness-626081f` |
| Code review | `t_a4855c01` | #86 | `code-reviewer` | `/home/alexandre/LoLSaas/.worktrees/t_a4855c01` | `review/release-readiness-626081f` |

Nenhum dos dois gates publicou nada (sem push, merge, tag, release ou deploy).
Os números abaixo foram transcritos dos relatórios dos cards; quando um valor foi
reexecutado nesta consolidação, isso está marcado explicitamente na seção correspondente.
Não foram reaproveitados valores de outro SHA (a preparação histórica `t_6bd2fbde` correra
sobre `5fb34f7` e **não** serve de evidência para este candidato).

## 2. Gate 1 — QA (`t_508033d0`, run #85)

Workspace limpo (sem alterações não commitadas), no SHA candidato.

### Backend

| Cenário | Comando | Resultado | Status |
|---|---|---|---|
| Build | `dotnet build LoLCoach.slnx -v minimal` | 0 Warning(s), 0 Error(s), 62s | executado: passou |
| Testes completos | `dotnet test LoLCoach.slnx --no-build -v minimal` | 75/75 passed, 0 failed, 0 skipped, 35s | executado: passou |
| Format check | `dotnet format LoLCoach.slnx --verify-no-changes --no-restore` | nenhuma alteração necessária | executado: passou |
| Smoke host | `python3 scripts/smoke_host.py` | `GET /` → 404, `POST /api/players/search` → 400 | executado: passou |
| Auditoria de vulnerabilidades | `dotnet list package --vulnerable` | registrado como "0 vulnerabilidades", porém **citado por engano como vindo de `npm audit`** (gap de evidência resolvido no gate 2 — ver §4) | executado com lacuna de citação |

**Migrations:** 2 existentes — `InitialPlayers` (20260911) e `AddMatches` (20260914) — sem alterações.
Tabelas `players`, `matches`, `player_matches` com constraints adequadas.

### Frontend

| Cenário | Comando | Resultado | Status |
|---|---|---|---|
| Install deps | `npm ci --include=dev` | 387 packages, 0 vulnerabilities | executado: passou |
| Build de produção | `ng build --configuration production` | 309.52 kB initial, lazy chunks (player-search, player-dashboard) | executado: passou |
| Testes | `ng test --watch=false` | 24/24 passed, 0 failed, 0 skipped | executado: passou |
| Auditoria npm | `npm audit` | 0 vulnerabilities | executado: passou |

**Nota de execução:** `vitest run` direto falha com "JIT compilation failed" e
"TestBed.initTestEnvironment() first" — comportamento esperado do Angular 22 + Vitest.
O comando canônico é `ng test --watch=false` (via Angular builder).

### CI/CD

| Workflow | Avaliação | Status |
|---|---|---|
| `deploy.yml` | PLACEHOLDER confirmado — apenas `workflow_dispatch`, apenas `echo`, nenhum passo real de deploy | executado: passou (seguro) |
| `backend-ci.yml` | CI correto: build, test, format check | executado: passou |
| `frontend-ci.yml` | Funcional com guards `if: hashFiles(...)`, porém comentários de cabeçalho desatualizados | análise estática: risco baixo |
| `python-ci.yml` | Syntax check simples | executado: passou |

### Secrets/configuração

- Nenhum segredo exposto no código; `${{ secrets.* }}` em `deploy.yml` aparece apenas em comentários/placeholders.
- `.env.example` comentado, sem valores reais.
- Referências a "password/secret/token" no código são apenas a variável `token` de parse de percentual
  (GeminiAiCoach) e `cancellationToken` (PlayersController).

### Veredito QA: APROVADO

## 3. Gate 2 — Code review (`t_a4855c01`, run #86)

O revisor **não aceitou o relato do QA como prova** e reproduziu os cenários de forma
independente no workspace `review/release-readiness-626081f` (working tree limpo, sem
diff contra `develop` e sem sementes de correção).

### 13 cenários reproduzidos nesta sessão de review

| # | Cenário | Comando | Resultado reproduzido | QA reportado | Match |
|---|---|---|---|---|---|
| 1 | Backend build | `dotnet build LoLCoach.slnx -v minimal` | 0 Warning(s), 0 Error(s), 9.91s | 0W/0E, 62s | OK |
| 2 | Backend testes | `dotnet test LoLCoach.slnx --no-build -v minimal` | Passed: 75, Failed: 0, Skipped: 0, 35s | 75/75 | OK |
| 3 | Backend format | `dotnet format LoLCoach.slnx --verify-no-changes --no-restore` | sem output (= sem alterações) | no changes needed | OK |
| 4 | Auditoria de vulnerabilidades do backend (**gap do orquestrador**) | `dotnet list LoLCoach.slnx package --vulnerable --include-transitive` | Api: "no vulnerable packages" / Tests: "no vulnerable packages" — saída real | descrito como "via npm audit" | OK (gap resolvido) |
| 5 | Frontend install | `npm ci --include=dev` | 387 packages (apenas avisos de install-scripts — não é vulnerabilidade) | 387 packages, 0 vulnerabilities | OK |
| 6 | Frontend build | `ng build --configuration production` | Initial total 309.52 kB; lazy chunks player-search/player-dashboard; 25.121s | 309.52 kB initial | OK |
| 7 | Frontend testes | `ng test --watch=false` | Test Files 5 passed / Tests 24 passed / 6.40s | 24/24 | OK |
| 8 | npm audit | `npm audit --omit=dev` | found 0 vulnerabilities | 0 vulnerabilities | OK |
| 9 | Smoke host | `python3 scripts/smoke_host.py` | `GET /` → 404, `POST /api/players/search` → 400 | idem | OK |
| 10 | `deploy.yml` seguro | leitura estática | apenas `workflow_dispatch: {}`, só `echo`, nenhum passo real, secrets só em comentários | placeholder confirmado | OK |
| 11 | Secrets expostos | grep de padrões sensíveis em código+config | 0 matches reais | nenhum | OK |
| 12 | Migrations intactas | `git log -- migrations/` | 2 migrations (`InitialPlayers` 20260911, `AddMatches` 20260914), sem commit no SHA atual | 2, unchanged | OK |
| 13 | Workspace limpo | `git status` | working tree clean, sem diff vs `develop` | n/a | OK |

### Achados do review

- **Bloqueantes: 0**
- **Correções obrigatórias: 0**
- **Melhorias recomendadas: 1** — relatório do QA citou `npm audit` como fonte do resultado de `dotnet list package --vulnerable`
  (inconsistência de evidência, sem risco técnico; o comando real foi executado pelo revisor e confirma 0 vulnerabilidades).
- **Sugestões opcionais: 2** — cabeçalho obsoleto de `frontend-ci.yml`; documentar o comportamento fail-closed do CORS.

### Veredito code review: APROVADO

## 4. Auditoria de vulnerabilidades do backend (saída real)

Comando:

```bash
cd backend
dotnet list LoLCoach.slnx package --vulnerable --include-transitive
```

Saída real (reexecutada durante esta consolidação, no worktree desta branch, em 2026-09-18):

```text
  Determining projects to restore...
  Restored /home/alexandre/LoLSaas/.worktrees/t_7fae66cb/backend/src/LoLCoach.Api/LoLCoach.Api.csproj (in 1.28 sec).
  Restored /home/alexandre/LoLSaas/.worktrees/t_7fae66cb/backend/tests/LoLCoach.Tests/LoLCoach.Tests.csproj (in 1.25 sec).

The following sources were used:
   https://api.nuget.org/v3/index.json

The given project `LoLCoach.Api` has no vulnerable packages given the current sources.
The given project `LoLCoach.Tests` has no vulnerable packages given the current sources.
```

Resultado: **0 pacotes vulneráveis** (diretos ou transitivos) em `LoLCoach.Api` e `LoLCoach.Tests`.
Isto fecha a lacuna de evidência apontada pelo orquestrador no card do QA: o resultado agora
tem comando e saída próprios, não é citação de `npm audit`.

## 5. Tabela de veredito dos dois gates

| Gate | Card / Run | Perfil | SHA validado | Passed | Failed | Skipped | Veredito |
|---|---|---|---|---|---|---|---|
| QA (Release Readiness) | `t_508033d0` / #85 | `qualidade` | `626081f` | 75 backend + 24 frontend + build/format/smoke/audit | 0 | 0 | **Aprovado** |
| Code review | `t_a4855c01` / #86 | `code-reviewer` | `626081f` | 13 cenários reproduzidos | 0 | 0 | **Aprovado** |

## 6. Riscos residuais e pendências com dono

Nenhum item abaixo bloqueia o gate de release; todos são dívida documental/configuração.

| # | Item | Classificação | Dono sugerido | Condição de fechamento |
|---|---|---|---|---|
| 1 | `.github/workflows/frontend-ci.yml:1-7` — cabeçalho diz que "o frontend AINDA NÃO existe"; os passos reais já usam guards `if: hashFiles('frontend/package.json') != ''` e funcionam | Dívida documental (QA classificou como risco baixo; review reclassificou como sugestão opcional — impacto técnico zero) | `dev-backend` | Atualizar o header; **fora do escopo desta task** (item da Spec 008) |
| 2 | `backend/src/LoLCoach.Api/appsettings.json:9-11` — `CORS.AllowedOrigins: ""` | Não é falha de segurança: `Program.cs:86` aplica `SetIsOriginAllowed(_ => false)` fora de Development (fail-closed, nunca `AllowAnyOrigin`). É lacuna de configuração de deploy: clientes browser não chamam a API em produção até `CORS__AllowedOrigins` ser definido | `dev-backend` + validação cruzada com a Spec 008 Railway (`dev-backend`/`t_80bda245`) | Documentar o fail-closed e garantir a env var na Spec 008; **fora do escopo desta task** |
| 3 | Relatório de QA citou `npm audit` como fonte do `dotnet list package --vulnerable` | Inconsistência de evidência (sem risco técnico) | `qualidade` | Citar o comando exato por resultado em relatórios futuros |
| 4 | Spec 008 Railway (deploy real) | Pendência de escopo, não validada aqui | `orquestrador` / `devops` | Decisão de promoção e despacho do deploy; worktrees `t_80bda245` / `t_d214248c` |
| 5 | Promoção de `develop` para `homologacao`/`main` e qualquer deploy | Fora do escopo desta Fase 8 | `orquestrador` (decisão) + `github-profile`/`devops` (execução autorizada) | Autorização específica do usuário; não concedida nesta fase |

## 7. Limitações e riscos residuais da validação

Registrado explicitamente, conforme exigido pelo card:

- **A validação foi local. Não houve deploy real.** Nenhum ambiente de produção ou homologação foi tocado.
- Os testes de integração usam **Testcontainers** (PostgreSQL efêmero, descartável). Eles **não** validam
  Supabase nem Railway; os testes usam clientes fake e dados sintéticos, sem chamadas reais à Riot API ou ao Gemini.
- Não foram executados mutation testing, fuzzing ou análise dinâmica (DAST).
- Não houve revisão de código linha a linha: o escopo do gate é prontidão de release, não o code review
  individual das specs — as specs 001–007 já passaram por QA e code review formais anteriormente.
- Não houve validação no navegador em ambiente implantado (sem UI publicada).
- Credenciais Riot/Gemini/Railway/Supabase não foram usadas.

## 8. Correções documentais incluídas nesta branch

Além desta evidência, a branch `docs/release-readiness-626081f` transporta 4 correções
documentais que estavam modificadas e não commitadas no checkout principal, aplicadas por
patch e conferidas byte a byte (sha256 idêntico ao estado do checkout principal):

| Arquivo | Correção |
|---|---|
| `README.md` | Spec 007 incluída na estrutura e na ordem recomendada |
| `backend/README.md` | Descrição do uso atual de FluentValidation, HttpClientFactory e do `HttpClient` do AI Coach/Gemini; remoção das afirmações obsoletas (ausência de integração Riot, frontend/QA/review pendentes, uso futuro de FluentValidation/HttpClientFactory) |
| `specs/001-player-search/design.md` | Removida a nota final obsoleta ("QA, frontend e review pendentes") |
| `specs/006-ai-coach/spec.md` | Identificador do modelo confirmado como `gemini-3.8`; Open Questions sem pendências |

`frontend/angular.json` **não** faz parte desta branch: é alteração local do usuário no
checkout principal e permanece apenas local (confirmado com `git show --stat` do commit).

## 9. Como reproduzir

```bash
git fetch origin
git checkout docs/release-readiness-626081f   # ou: gh pr checkout <PR>
git rev-parse HEAD && git log -1 --format='%H %s'

# Backend
cd backend
dotnet build LoLCoach.slnx -v minimal
dotnet test LoLCoach.slnx --no-build -v minimal
dotnet format LoLCoach.slnx --verify-no-changes --no-restore
dotnet list LoLCoach.slnx package --vulnerable --include-transitive
cd ../ && python3 scripts/smoke_host.py

# Frontend
cd frontend
npm ci --include=dev
ng build --configuration production
ng test --watch=false
npm audit --omit=dev
```

## 10. Conclusão

O candidato `develop` @ `626081f3f9b6c81fe95576d9b8ad2f4aab50fd16` passou pelos dois gates
independentes da Fase 8 — QA (run #85) e code review (run #86) — com veredito **Aprovado** em
ambos, **0 achados bloqueantes** e **0 correções obrigatórias**. A validação é local
(Testcontainers, sem deploy real) e os riscos residuais são de documentação/configuração,
com dono definido na §6.

Esta branch é **documentação**: não publica, não faz merge, não cria tag/release e não faz deploy.
A promoção de `develop` e o deploy real (Spec 008 Railway) exigem autorização específica do usuário.
